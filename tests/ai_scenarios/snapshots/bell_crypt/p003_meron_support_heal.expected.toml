# Support priority: Человек Настоятель Мерон r1, hp=10/18, ap=1/1,
# intent=FocusTarget. Союзник 34359738023 (hp=8/12) ранен — цель для heal.
# Ожидание: AI-поддержка коммитит heal на раненого союзника, не идёт в атаку.
#
# Это тест того, что support-роль корректно оценивает value лечения
# против атаки при наличии раненного ally.
#
# Source: rebuilt из v27 logs 20260426 (bell_crypt). v27: plan_id = actor_id.
# v30 rebuild (2026-04-28): actor entity ID изменился в новом playtest
# (55834574500 → 34359738029), поведение идентично: Мерон кастует heal на ally hp=4/12.

[scope]
plan_id = 34359738029

[[expectations]]
cast_ability = ["heal"]
primary_effect = ["Heal"]
