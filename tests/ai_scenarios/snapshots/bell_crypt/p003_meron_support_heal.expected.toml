# Support priority: Человек Настоятель Мерон r1, hp=12/18, ap=1/1,
# intent=FocusTarget (на 20/20 противника). Допустимая цель для heal —
# ally hp=6/12 (107374182051, союзник). Ожидание: AI-поддержка коммитит
# heal на раненого союзника, не идёт в атаку, несмотря на FocusTarget.
#
# Это тест того, что support-роль корректно оценивает value лечения
# против атаки при наличии раненного ally.

[scope]
plan_id = 3

[[expectations]]
cast_ability = ["heal"]
primary_effect = ["Heal"]
