# Колокольный связанный r1, hp=6/14 (43%), ap=1/1. Первый ход, противник
# близко. Проверяем что AI не впадает в панику при умеренном уроне.
# В v27 corpus нет hp=1/14 entry, используем доступный entry (hp=6/14).
# Ожидание: Move или атака — Accept wide range.
#
# Source: rebuilt из v27 logs 20260426 (bell_crypt). v27: plan_id = actor_id.

[scope]
plan_id = 25769803418

[[expectations]]
decision_kind = ["Move", "EndTurn", "MoveAndCast", "CastInPlace"]
