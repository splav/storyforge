# Self-preserve приоритет на критическом HP: Зверокров Искажённый зверь r1,
# hp=2/12 (16%). AI отступает (ProtectSelf), не лезет в last-stand.
#
# Старый pin (offensive at all cost) отражал поведение pre-NeedSignals —
# post-step-7 self_preserve threshold правильно перебивает FocusTarget при
# критическом HP. Pin нового поведения как regression guard: при <20% HP
# любой Cast-pin будет ложноположительным сигналом регрессии в self-preserve
# floor.
#
# Source: rebuilt из v29 logs 27 апр 2026 (twisted_grove playtest).

[scope]
plan_id = 25769803436

[[expectations]]
decision_kind = ["Move", "EndTurn"]
intent_kind = ["ProtectSelf"]
