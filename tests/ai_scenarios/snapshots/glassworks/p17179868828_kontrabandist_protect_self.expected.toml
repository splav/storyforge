# Self-preserve приоритет на критическом HP: Контрабандист r=1, hp=1/10 (10%).
# AI перемещается в ProtectSelf вместо атаки несмотря на наличие AP.
# intent_reason.kind = "panic_override" (self_preserve=0.96 > threshold=0.85).
#
# Этот pin защищает от регрессии в self_preserve floor: если порог понизить
# ниже 10% HP, AI будет атаковать вместо отхода — поведение корректно только
# при наличии defensive options.
#
# Source: glassworks playtest 2026-04-28 (v30), round=1, actor=17179868828.

[scope]
plan_id = 17179868828

[[expectations]]
decision_kind = ["Move", "EndTurn"]
intent_kind = ["ProtectSelf"]
