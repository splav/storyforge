# Continuation: Relevant mismatch — goal preserved, но critical HP перебивает.
#
# Actor: Человек Контрабандист (17179868828), r=1, hp=1/10 (10%).
# Stored goal (injected): Pressure on target 17179868830.
# Mismatch: target moved [3,4]→[1,2] → severity=Relevant (движение < 3 hex).
#
# Pre_tick поведение: Relevant severity сохраняет goal в AiMemory (repair
# affinity ещё работает). НО self_preserve floor перебивает FocusTarget при
# 10% HP — actor выбирает Move + ProtectSelf вместо атаки. Это корректное
# взаимодействие двух систем: continuation хранит контекст, NeedSignals
# определяет тактический выбор.
#
# Сценарий validates что Relevant injection не падает / не зависает / не
# заставляет actor'а слепо выполнить goal-cast несмотря на критический HP.
#
# Source: glassworks playtest 2026-04-28 (v30), round=1, actor=17179868828.

[scope]
plan_id = 17179868828

[ai_memory]
last_goal_kind = "Pressure"
last_goal_target = 17179868830
last_goal_region_anchor = [3, 4]
last_goal_region_radius = 2
last_goal_ttl = 2
last_goal_confidence = 1.0
last_goal_created_round = 0
last_goal_expected_actor_pos = [5, 1]
last_goal_actor_hp_at_store = 8
last_goal_actor_rage_at_store = 0
last_goal_actor_status_hash = "15130871412783076140"
last_goal_target_hp_at_store = 20
last_goal_target_pos_at_store = [3, 4]

[[expectations]]
# Critical HP overrides preserved goal — self_preserve берёт верх.
decision_kind = ["Move", "EndTurn"]
intent_kind = ["ProtectSelf"]
