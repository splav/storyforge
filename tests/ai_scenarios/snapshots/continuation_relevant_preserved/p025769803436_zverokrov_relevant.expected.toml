# Continuation: Relevant mismatch — goal preserved.
#
# Actor: Зверокров Искажённый зверь (25769803436), r2, hp=3/12.
# Stored goal: Pressure on target 17179868827.
# Mismatch: target moved from [0,3] to [2,4] → severity=Relevant.
# Expected: actor still attacks the same target (goal preserved despite mismatch).
#
# This validates that Relevant severity does NOT by itself abandon a goal.
# The actor proceeds to MoveAndCast on the same target (17179868827).
#
# [ai_memory] injects the stored goal so the runner knows it existed at this tick.
# Without injection, AiMemory.last_goal=None → no repair_affinity bonus.
# With injection, repair_affinity bonus still consistent because actor goes for
# same target regardless (high score margin from damage potential at 3 hp).
#
# Source: twisted_grove playtest 2026-04-26, round=2, actor=25769803436.
# continuation.severity = Relevant, age = 1.

[scope]
plan_id = 25769803436

[ai_memory]
last_goal_kind = "Pressure"
last_goal_target = 17179868827
last_goal_region_anchor = [0, 3]
last_goal_region_radius = 2
last_goal_ttl = 2
last_goal_confidence = 1.0
last_goal_created_round = 1
last_goal_expected_actor_pos = [3, 5]
last_goal_actor_hp_at_store = 3
last_goal_actor_rage_at_store = 0
last_goal_actor_status_hash = "15130871412783076140"
last_goal_target_hp_at_store = 20
last_goal_target_pos_at_store = [0, 3]

[[expectations]]
# Actor at 25% HP may switch to ProtectSelf (survival) or continue FocusTarget.
# Key: pre_tick does NOT clear the goal (Relevant severity), so the actor still
# attacks the same target regardless of intent override.
decision_kind = ["MoveAndCast", "CastInPlace"]
cast_ability = ["melee_attack"]
cast_target = [17179868827]
primary_effect = ["Damage"]
