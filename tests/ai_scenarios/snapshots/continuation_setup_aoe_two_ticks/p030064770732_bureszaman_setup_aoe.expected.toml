# Continuation: SetupAOE goal — actor casts planned AoE ability.
#
# Actor: Бурешаман (30064770732), r2, hp=30/30, mana=[6,6].
# Stored goal (injected via [ai_memory]): SetupAOE with planned_ability=thunderstrike,
#   region_center=[1,3] (actor is already at [1,3] — in position for AoE blast).
# Two enemy clusters near [-2,3] and [0,3] make thunderstrike optimal.
# Expected: actor casts thunderstrike (ground-targeted AoE covering multiple enemies).
#
# The SetupAOE goal signals that the actor previously moved toward a position
# to cast this AoE, and now executes the cast from that position.
# Repair_affinity bonus on the thunderstrike plan confirms the committed goal.
#
# Source: stormborn_camp playtest 2026-04-26, round=2, actor=30064770732.

[scope]
plan_id = 47244639911

[ai_memory]
last_goal_kind = "SetupAOE"
last_goal_region_anchor = [1, 3]
last_goal_region_radius = 2
last_goal_planned_ability = "thunderstrike"
last_goal_ttl = 2
last_goal_confidence = 1.0
last_goal_created_round = 1
last_goal_expected_actor_pos = [1, 3]
last_goal_actor_hp_at_store = 14
last_goal_actor_rage_at_store = 0
last_goal_actor_status_hash = "15130871412783076140"
last_goal_target_hp_at_store = 0
last_goal_target_pos_at_store = [0, 0]

[[expectations]]
decision_kind = ["CastInPlace", "MoveAndCast"]
cast_ability = ["thunderstrike"]
primary_effect = ["Damage"]
