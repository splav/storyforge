# Continuation: SetupAOE injection не перебивает rescue_ally need.
#
# Actor: Грозорождённый Старшина (47244639911), r=2, hp=22/22.
# Injected SetupAOE memory с planned_ability=thunderstrike в позиции [1,3].
# Реальное состояние tick'а: ally 111669149350 при hp=1/14 (7%) —
# критический rescue_ally NeedSignal.
#
# AI выбирает CastInPlace heal на ally вместо thunderstrike. Это корректный
# приоритет: rescue_ally + actor-as-support axis weight перевешивают
# SetupAOE repair_affinity bonus.
#
# Сценарий validates что SetupAOE injection не заставляет actor'а слепо
# спамить thunderstrike при наличии раненых allies — NeedSignals принимают
# финальное решение.
#
# Source: stormborn_camp playtest 2026-04-28 (v30).
# v30 rebuild: actor hp=22 (полный), ally entity изменился → hp_at_store обновлён.
#
# Scope: overlay покрывает только ПЕРВЫЙ tick actor=34359738005 в логе (round 1).
# В round 3 этого же лога baseline_v36 ушёл из `cast heal` в `move_and_cast melee_attack`
# после Phase D Pass 3 (685a4cb): rescue_ally need signal перестал считать мёртвых
# союзников endangered allies. В round 3 у actor'а оба ally trупы (90194312866, 55834574506),
# rescue-сигнал корректно = 0, и AI добивает раненого Player (47244639898 hp=4/12).
# Это accidentally-fixed bug, не регрессия — см. tests rescue_ally_ignores_dead_allies_in_reach
# в src/combat/ai/appraisal/rescue_ally.rs.

[scope]
plan_id = 34359738005

[ai_memory]
last_goal_kind = "SetupAOE"
last_goal_region_anchor = [1, 3]
last_goal_region_radius = 2
last_goal_planned_ability = "thunderstrike"
last_goal_ttl = 2
last_goal_confidence = 1.0
last_goal_created_round = 1
last_goal_expected_actor_pos = [1, 3]
last_goal_actor_hp_at_store = 22
last_goal_actor_rage_at_store = 0
last_goal_actor_status_hash = "15130871412783076140"
last_goal_target_hp_at_store = 0
last_goal_target_pos_at_store = [0, 0]

[[expectations]]
# rescue_ally need перебивает SetupAOE — actor лечит, не атакует AoE.
decision_kind = ["CastInPlace", "MoveAndCast"]
cast_ability = ["heal"]
primary_effect = ["Heal"]
